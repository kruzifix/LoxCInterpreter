// lists: [val1, val2, ...], [] + [], [] * num, list[num]
// maps: { key: value, ... }, map[key]

// foreach loop/iterator?
// each(it in list) {}

// array assignment!

// read files!
// readLines(path)

var asdf = {
  count: 1,
  test: "blabla",
  ar: [1, 2, 3]
};

print(asdf);

print(asdf["count"]);

var ar = [1, 2, 3, 4, 5, 6];
print(ar);
