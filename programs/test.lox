// lists: [val1, val2, ...], [] + [], [] * num, list[num]
// maps: { key: value, ... }, map[key]

// foreach loop/iterator?
// each(it in list) {}

// array assignment!

// read files!
// readLines(path)

var numbers = [1, 2, 3, 4];

var it = 0;

each (var it in numbers)
{
  var abc = 0;
  //print(it);
  print(abc);
}
